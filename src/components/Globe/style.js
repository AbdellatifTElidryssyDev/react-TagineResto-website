import styled from "styled-components";

export const SVGNAV = styled.svg`
  transform: rotate(-37deg);

  #tagin-icon {
    fill: #fff;
    font-size: 10px;
  }
  &:hover {
    fill: #b0876a;
  }
`;
